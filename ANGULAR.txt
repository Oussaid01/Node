ANGULAR : 

setup:

npm install -g @angular/cli


to run the app :

ng serve


to create a component : 

ng generate component Home --standalone --inline-template


ng generate component HousignLocation --standalone --inline-template


To create a new type that represents our data by creating an interface : contract between us & the date => tell us we can expect the data
    to follow the structure defined the interface 




last one part 5